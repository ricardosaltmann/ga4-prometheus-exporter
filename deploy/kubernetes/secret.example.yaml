# ==============================================================================
# ATENÇÃO: NUNCA commite secrets com chaves reais no repositório!
# Este é apenas um template estrutural para o seu cluster Kubernetes.
# ==============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: ga4-exporter-credentials
  labels:
    app.kubernetes.io/name: ga4-prometheus-exporter
type: Opaque
stringData:
  # Insira o conteúdo do seu arquivo service-account.json aqui ao aplicar
  service-account.json: |
    {
      "type": "service_account",
      "project_id": "example-gcp-project",
      "client_email": "ga4-exporter@example-gcp-project.iam.gserviceaccount.com"
    }
